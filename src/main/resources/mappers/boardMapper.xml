<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boardMapper">
   
    <!-- 게시판 글 작성 -->
   <insert id="insert">
	   INSERT INTO gBoard(    gNo 
	   					      , gTitle 
	   					      , gWriter
	   					      , gContents 
	   					      )
	                 VALUES(    gBoard_SEQ.NEXTVAL 
	                 		  , #{gTitle}
	                 		  , #{gWriter}
	                 		  , #{gContents}
	                 		    )
   
   </insert>
   
   	<select id="list" resultType="org.comstudy21.vo.BoardVO">
		SELECT   gNo
			   , gTitle
			   , gContents
			   , gWriter
			   , gDate
			   
		  FROM gBoard
		  ORDER BY gNo DESC
	</select>
	
	<select id="read" parameterType="int" resultType="org.comstudy21.vo.BoardVO">
		SELECT gNo
			, gTitle
			, gContents
			, gWriter
			, gDate
		from gBoard
		where gNo = #{gNo}
	
	</select>
	
	<update id="update" parameterType="org.comstudy21.vo.BoardVO">
		UPDATE gBoard
		 SET gTitle = #{gTitle},
		 	 gContents = #{gContents}
		 WHERE gNo = #{gNo}
	</update>
	
	<delete id="delete" parameterType="int">
		DELETE
		 FROM gBoard
		 WHERE gNo = #{gNo}
	</delete>
</mapper>